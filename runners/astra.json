{
    "name": "astra",
    "command": "python",
    "args": [
        "drivers/astr.py",
        "--width", "${width}",
        "--num-projections", "${num_projections}",
        "--num-slices", "${num_slices}"
    ]
}
